import { style } from 'typestyle'

const wrapper =
  style
  ( { backgroundColor: '#444'
    , height: '100vh'
    , width: '100vw'
    , margin: 0
    }
  )

const logo =
  style
  ( { height: '30em'
    , width: '30em'
    }
  )

const styles =
  { wrapper
  , logo
  }

export
{ styles
}
